/**
 * 监测页面(Monitor)
 */
import React, {PureComponent} from 'react'

export default class Monitor extends PureComponent{
  // 页面渲染
  render(){
    return(
      <div>
        Monitor
      </div>
    );
  }
}
